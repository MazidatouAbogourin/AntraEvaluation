{
  "name": "antra-evaluation",
  "version": "1.0.0",
  "description": "",
  "main": "Controller.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3000",
    "dev": "live-server --ignore=db.json ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
